import React from 'react';

const Panel = ({ title }) => {
  return (
    <main className="section">
      <h2>{title}</h2>
      {/* Contenido de la sección */}
    </main>
  );
};

export default Panel;
